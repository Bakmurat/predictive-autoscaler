apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ml-engine
resources:
  - archive.yaml
configMapGenerator:
  - name: evidence-archive-code
    files:
      - archive.py
    options:
      labels: {app: evidence-archive, component: instrumentation}
