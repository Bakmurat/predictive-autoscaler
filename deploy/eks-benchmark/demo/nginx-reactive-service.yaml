---
apiVersion: v1
kind: Service
metadata:
  name: nginx-reactive
  namespace: demo
  labels:
    app: nginx-reactive
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx-reactive
