---
apiVersion: v1
kind: Service
metadata:
  name: myapptwo
  namespace: demo
  labels:
    app: myapptwo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: myapptwo
