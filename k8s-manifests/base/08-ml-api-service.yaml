apiVersion: v1
kind: Service
metadata:
  name: ml-api-service
  namespace: ml-engine
  labels:
    app: ml-api
spec:
  selector:
    app: ml-api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
